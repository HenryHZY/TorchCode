# User Solutions

The standalone web UI saves edited code for each task in this directory as
`<task_id>.py`. These files are intentionally tracked by Git so you can commit
and sync your work across machines.
